spring:
  application:
    name: ddd-example

  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  hibernate:
    ddl-auto: update

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
